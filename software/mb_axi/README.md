# LEGACY — nie używać w V1

Ten katalog zawiera wczesny przykład `main.c` dla MicroBlaze (prosty odczyt `FREQ_HZ`, UART **115200**).

**Aktywny firmware V1:** `sw/v1_uart/` + gotowy `firmware/ro_ring_app.elf`.

Zachowany wyłącznie jako archiwum rozwojowe projektu SDUP aring_osc.
